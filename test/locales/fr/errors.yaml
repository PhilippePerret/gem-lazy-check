---
10: |
  Le message d'erreur attendu n'est pas le bon.
  Attendu : %{e}
  Obtenu  : %{a}
100: |
  Le test devrait échouer (avec les données %{c}).
101: |
  Le test devrait réussir (avec les données %{c}).

200: |
  %{a} n'est pas une source valide. Il faut fournir soit une adresse internet,
  donc une URL (avec ou non des arguments) soit un code HTML valide (donc qui
  doit commencer par '<' et s'achever par '>'). Rien d'autre.
